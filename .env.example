# Copie para `.env` e preencha. O `.env` não é versionado.

# ID de medição do GA4 (Admin > Fluxos de dados > Web), formato G-XXXXXXXXXX.
# Deixe vazio ou remova a linha para rodar sem analytics — é o padrão em
# desenvolvimento, para não misturar testes com os dados de produção.
VITE_GA_MEASUREMENT_ID=
